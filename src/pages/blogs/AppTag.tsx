import React from "react";
import { BlogTagLayout, BlogWrapper } from "../../components/blog";

const AppTag = () => {
  return (
    <BlogWrapper>
      <BlogTagLayout />
    </BlogWrapper>
  );
};

export default AppTag;
