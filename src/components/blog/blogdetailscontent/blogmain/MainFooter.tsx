import React from "react";
import {
  textStyles,
  themes,
  flexLayout,
} from "../../../../styles/themes/theme";

const MainFooter = () => {
  const {} = textStyles;
  const { containerWrapper, themeWrapper } = themes;
  const {} = themeWrapper;
  const {} = flexLayout;
  return (
    <>
      <div></div>
    </>
  );
};

export default MainFooter;
