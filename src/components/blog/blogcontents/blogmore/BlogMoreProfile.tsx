import React from "react";
import {
  textStyles,
  themes,
  flexLayout,
} from "../../../../styles/themes/theme";

const BlogMoreProfile = () => {
  const {} = textStyles;
  const { themeWrapper, boxFull, XFull } = themes;
  const {} = themeWrapper;
  const { flexRowCenter, flexCol, flexRowCenterBetween } = flexLayout;
  return (
    <>
      <div className={`px-4 py-8 bg-white`}></div>
    </>
  );
};

export default BlogMoreProfile;
